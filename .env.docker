POSTGRES_DB=traffic_gt
POSTGRES_USER=traffic_user
POSTGRES_PASSWORD=traffic_pass

POSTGRES_HOST_PORT=55432
BACKEND_HOST_PORT=8000
FRONTEND_HOST_PORT=4200
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443
SITE_HOST=localhost
CADDYFILE_PATH=./deploy/Caddyfile.local

# false = la API retorna error real si falla la DB (no datos mock)
API_ALLOW_MOCK=false
